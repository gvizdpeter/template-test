name: Run release

on:
  workflow_run:
    workflows: ["Add deploy labels"]
    types: [completed]

  pull_request:
    types: [edited, synchronize]

jobs:
  run-release:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
    - uses: actions/checkout@v3
    - name: Run release
      run: |
        echo "${{ toJSON(github) }}"